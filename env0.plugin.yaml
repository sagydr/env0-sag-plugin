name: Sag-Plugin-Bucket-Name
icon: https://avatars.githubusercontent.com/u/9664064?v=4
inputs:
  plan:
    description: PR Plan as JSON
    required: true
  plan_path:
    description: JSON path
    required: true

run:
  exec: |
    ls && echo "${inputs.plan_path}" && echo "${inputs.plan}"
#    chmod +x run.sh && ./run.sh && echo "${inputs.plan_path}" && echo "${inputs.plan}"